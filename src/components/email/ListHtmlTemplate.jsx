import React from "react";
import { Link } from "react-router";

export const ListHtmlTemplate = () => {
  // Временный
  // const []

  return (
    <div>
      <Link to={`/newhtml`}>
        <button>+</button>
      </Link>
    </div>
  );
};
